<div class="jumbotron">
  <h1>Blocipedia</h1>
  <h3> Share your knowledge with the world </h3>
  <p>
    <% if user_signed_in? %>
      <p> Welcome to Blocipedia, <%= current_user.name %> </p>
    <% else %>
      <p>
        <%= link_to "Sign Up", new_user_registration_path, class: 'btn-primary btn-lg'%>
        <%= link_to "Sign In", new_user_session_path, class: 'btn-success btn-lg' %>
      </p>
      <% end %>
  </p>
  </div>
